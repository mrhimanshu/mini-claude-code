<!-- sourcemap:start -->
# SourceMap AI MCP

This project is indexed by SourceMap AI as **mini-claude-code** (334 symbols, 1019 relationships, 27 execution flows).

## Always Start Here

1. **Read `sourcemap://repo/{name}/context`** — codebase overview + check index freshness
2. **Match your task to a skill below** and **read that skill file**
3. **Follow the skill's workflow and checklist**

> If step 1 warns the index is stale, run `npx sourcemap analyze` in the terminal first.

## Skills

| Task | Read this skill file |
|------|---------------------|
| Understand architecture / "How does X work?" | `.claude/skills/sourcemap/sourcemap-exploring/SKILL.md` |
| Blast radius / "What breaks if I change X?" | `.claude/skills/sourcemap/sourcemap-impact-analysis/SKILL.md` |
| Trace bugs / "Why is X failing?" | `.claude/skills/sourcemap/sourcemap-debugging/SKILL.md` |
| Rename / extract / split / refactor | `.claude/skills/sourcemap/sourcemap-refactoring/SKILL.md` |
| Tools, resources, schema reference | `.claude/skills/sourcemap/sourcemap-guide/SKILL.md` |
| Index, status, clean, wiki CLI commands | `.claude/skills/sourcemap/sourcemap-cli/SKILL.md` |

<!-- sourcemap:end -->